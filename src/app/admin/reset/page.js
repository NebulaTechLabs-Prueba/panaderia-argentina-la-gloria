import { Reset } from "@/modules/admin/Reset";

export const metadata = {
  title: "Restablecer contraseña · Panel La Gloria",
  robots: { index: false, follow: false },
};

export default function ResetPage() {
  return <Reset />;
}
