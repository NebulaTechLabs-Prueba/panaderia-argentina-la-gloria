import { Login } from "@/modules/admin/Login";

export const metadata = {
  title: "Ingresar · Panel La Gloria",
  robots: { index: false, follow: false },
};

export default function LoginPage() {
  return <Login />;
}
